server:
  port: 8088
  shutdown: graceful

spring:
  application:
    name: shipment-handoff-service

  datasource:
    url: jdbc:postgresql://localhost:55432/iwos
    username: iwos
    password: iwos_secret
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: shipment_handoff
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: shipment_handoff
    table: flyway_schema_history
    baseline-on-migrate: false

  data:
    redis:
      host: localhost
      port: 56379
      password: iwos_secret
      timeout: 5000ms

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false

  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true
  metrics:
    tags:
      service: shipment-handoff-service
      env: ${ENVIRONMENT:local}

shipment-handoff-service:
  service-name: shipment-handoff-service
  kafka:
    consumer-topic: iwos.task-execution.events.v1
    producer-topic: iwos.shipment-handoff.events.v1
    group-id: shipment-handoff-service-consumer
  warehouse:
    base-url: http://localhost:8086
  order-intake:
    base-url: http://localhost:8081
  outbox:
    poll-interval-ms: 1000

logging:
  level:
    root: INFO
    com.iwos.shipmenthandoff: DEBUG
    org.springframework.kafka: WARN
    org.apache.kafka: WARN
