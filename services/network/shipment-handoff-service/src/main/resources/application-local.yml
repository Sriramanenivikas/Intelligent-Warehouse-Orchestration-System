spring:
  config:
    activate:
      on-profile: local

logging:
  level:
    com.iwos.shipmenthandoff: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      env: local
