apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-template.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
