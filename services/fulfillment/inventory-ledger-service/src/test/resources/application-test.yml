spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:postgresql://localhost:55432/iwos
    username: iwos
    password: iwos_secret
  data:
    redis:
      host: localhost
      port: 56379
      password: iwos_secret

logging:
  level:
    root: WARN
