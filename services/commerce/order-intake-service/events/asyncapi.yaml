asyncapi: 3.0.0
info:
  title: order-intake-service Events
  version: 0.1.0
  description: Planning skeleton only. No topics or channels implemented yet.
channels: {}
asyncapi: 3.0.0
info:
  title: Order Intake Accepted Event
  version: 1.0.0
channels:
  order-intake.accepted.v1:
    address: order-intake.accepted.v1
    messages:
      orderIntentAccepted:
        $ref: '#/components/messages/OrderIntentAccepted'
components:
  messages:
    OrderIntentAccepted:
      name: OrderIntentAccepted
      payload:
        type: object
        required: [eventId, aggregateId, eventType, occurredAt]
        properties:
          eventId:
            type: string
            format: uuid
          aggregateId:
            type: string
            format: uuid
          eventType:
            type: string
          occurredAt:
            type: string
            format: date-time
