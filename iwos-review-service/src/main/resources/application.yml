server:
  port: 8100
spring:
  application:
    name: iwos-review-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/iwos_reviews}
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
