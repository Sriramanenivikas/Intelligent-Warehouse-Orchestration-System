apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: iwos-public
spec:
  parentRefs:
    - name: iwos-edge
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
