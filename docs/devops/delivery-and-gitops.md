# Delivery And GitOps

Planned delivery model:

- PR validation through GitHub Actions
- artifact build and scanning in CI
- environment promotion through GitOps
- Argo CD application-of-applications pattern
- separate `dev`, `stage`, `perf`, and `prod` environment roots
